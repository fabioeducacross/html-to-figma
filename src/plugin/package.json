{
  "name": "html-to-figma-plugin",
  "version": "0.1.0",
  "description": "HTML-to-Figma Figma Plugin",
  "private": true,
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "vitest"
  },
  "dependencies": {
    "@figma/plugin-typings": "^1.90.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
